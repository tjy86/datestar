<p>All it takes is <strong>one</strong> right answer!</p>

<ul class="nav nav-list">
	<li class="divider"></li> 
</ul>


<div id="container_logged_in">

	<div class = "content">
		<div id="answers"></div>
	</div>

	<div class = "divider_box">
	</div>
	<div class="answer">
		<%= form_for @a do |f| %>
			<%= f.label :correct_answer %>
			<%= f.text_field :answer1 %>

			<%= f.label :fake_answer %>
			<%= f.text_field :answer2 %>

			<%= f.label :fake_answer %>
			<%= f.text_field :answer3 %>

			<%= f.hidden_field :question_id, :value => (Question.where(:user_id => @authenticated_user.id).last if @authenticated_user).id %>

			<%= f.submit 'Save', class: "btn" %>
		<% end %>
	</div>

</div>